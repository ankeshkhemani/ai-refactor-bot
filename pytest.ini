[pytest]
markers =
    integration: marks tests that require external dependencies or environment variables
    pure: marks tests that only test pure functions without external dependencies

# By default, skip integration tests
addopts = -v -m "not integration"
